class Solution {
public:
    int arrangeCoins(int n) {
        int p = 1;
        int sum = 0;

        while (sum + p <= n) { 
            sum += p;
            p++;
        }

        return p - 1; 
    }
};
